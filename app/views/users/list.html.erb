<h1>List of Users</h1>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.email, user_path(user) %>, registered <%= time_ago_in_words(user.created_at) %> ago </li>
  <% end %>
</ul>
<%= link_to 'Back', root_path %>
